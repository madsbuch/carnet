{
  "name": "carnet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "dev:ui": "bun run src/dev.ts",
    "build:ui": "rm -rf dist && bun build src/client/index.html --outdir dist --minify",
    "test": "bun test"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-dialog": "^2.7.2",
    "@tauri-apps/plugin-opener": "^2.5.4",
    "marked": "^18.0.7"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/bun": "^1.3.14",
    "happy-dom": "^20.11.1"
  }
}
